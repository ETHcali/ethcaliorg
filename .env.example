# Public site — anon key only. There is no service-role key in this repo: the site
# never writes. Editing happens in wallet_ethcali /admin/content, behind an
# on-chain ADMIN_ROLE check.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...
